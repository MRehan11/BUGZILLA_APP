
   <script>
    $(document).ready(function() {

      $(':input[type="submit"]').prop('disabled', true);


      $('#project-title').keyup(function(){
        console.log("here in title")
        if($('#project-title').val() !== '' && $('#project-description').val() !== ''){
          $(':input[type="submit"]').prop('disabled', false);
        }
      });

      $('#project-description').keyup(function(){
        console.log("here in description")
        if($('#project-title').val() !== '' && $('#project-description').val() !== ''){
          $(':input[type="submit"]').prop('disabled', false);
        }
      });

      // $(':input[type="submit"]').onchange(function() {
      //     $('#project-title')
      //     if($(this).val() != '') {
      //       $(':input[type="submit"]').prop('disabled', false);
      //     }
      // });

    });
  </script>
    <div class="row">
      <div class="col-sm">
      </div>
      <div class="col-sm">
      </div>
      <div class="col-sm">
        <%= link_to 'Back To Projects', projects_path, :class => "btn btn-outline-primary" %>
      </div>
    </div>

    <div class="container d-flex justify-content-center text-primary">
      <h3>Create new Project</h3>
    </div>
    <div class="container d-flex justify-content-center">
      <%= render 'form' %>
    </div>